A command-line Python game where the user competes against the computer. 
# The game runs for 10 rounds(editable), tracks scores, and declares the final winner.
# Demonstrates use of functions, loops, conditionals, random module, and user input.
